"use client";
import SessionManager from "@/components/account/session";
import React from "react";

const Session = () => {
  return (
    <SessionManager className="bg-gradient-to-r from-blue-900/40 via-purple-800/40 to-purple-900/40 backdrop-blur-md text-white" />
  );
};

export default Session;
