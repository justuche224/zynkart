import React from "react";
import type { Store } from "../../_types";

interface HomeProps {
  store: Store;
}

const Home = ({ store }: HomeProps) => {
  return <div>Home</div>;
};

export default Home;
